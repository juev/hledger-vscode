{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Extension (Clean - No Other Extensions)",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "--disable-workspace-trust",
                "--disable-telemetry",
                "--no-sync",
                "--new-window"
            ],
            "outFiles": [
                "${workspaceFolder}/out/**/*.js"
            ],
            "timeout": 30000,
            "preLaunchTask": {
                "type": "npm",
                "script": "compile"
            }
        },
        {
            "name": "Run Extension (With Other Extensions)",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "outFiles": [
                "${workspaceFolder}/out/**/*.js"
            ],
            "timeout": 30000,
            "preLaunchTask": {
                "type": "npm",
                "script": "compile"
            }
        },
        {
            "name": "Run Extension Tests",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "--extensionTestsPath=${workspaceFolder}/out/test"
            ],
            "outFiles": [
                "${workspaceFolder}/out/**/*.js"
            ],
            "timeout": 30000,
            "preLaunchTask": {
                "type": "npm",
                "script": "compile"
            }
        }
    ]
}
